[project]
name = "clearml-gpu-test"
version = "0.1.0"
description = "GPU test with uv and clearml"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "clearml>=1.16.0",
    "torch>=2.2.0",
]